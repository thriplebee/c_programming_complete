#include <stdio.h>
// Explain the Speciality of Do While Loop
main(){
    int count = 5;

    // Do While
    do{
        printf("Number %d\n",count);
        count--;
    }while(count > 5);

    // Explanation : It prints count then checks the condition
    // But according to the condition that is wrong.
}

