#include <stdio.h>
// Do While Loop
// Do tasks first and then checks the condition
main(){
    int count = 0;

    // Do While
    do{
        printf("Number %d\n",count);
        count++;
    }while(count < 10);

    getch();
}
